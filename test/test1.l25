program test {                                                               
    func f1(a, b: int, c: [3,4], d) {
        let e: [30];
        return a + b + d;
    }
    func f2(a, b, c: int) {
        let d: [3, 4];
        return a + b + f1(a, b, d, c);
    }
    main {
        let a: [3, 4];
        let b = f1(1, 2, a, 1234) + f2(3, 4, 5);
        let c: int = b;
        output(b + c);
    }
}