program test {                                              
    func f1(a, b: int, c, d) {
        let e: [30];
        return a + b + d;
    }
    func f2(a, b, c: int) {
        let d: [30, 40, 50];
        func f3() {
            let e = 10;
            return 1;
        };
        return a + b + f1(a, b, f3(), 4);
    }
    main {
        let a: [30, 40, 50];
        let b = f1(1, 2, 3, 4) + f2(1, 2, 3);
        let c: int = b;
        let d = a[3, 4, 5];
        output(b+c);
    }
}